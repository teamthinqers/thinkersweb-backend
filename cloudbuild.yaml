steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '--no-cache', '-t', 'gcr.io/$PROJECT_ID/thinkersweb-backend:$COMMIT_SHA', '.']

  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/thinkersweb-backend:$COMMIT_SHA']

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'thinkersweb-backend'
      - '--image'
      - 'gcr.io/$PROJECT_ID/thinkersweb-backend:$COMMIT_SHA'
      - '--region'
      - 'europe-west1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--memory'
      - '512Mi'
      - '--set-env-vars'
      - 'NODE_ENV=production,FRONTEND_URL=https://www.thinqers.in'

images:
  - 'gcr.io/$PROJECT_ID/thinkersweb-backend:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY
